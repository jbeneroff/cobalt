import React from 'react';
import { IconProps } from '@uiw/react-icon';
import { IProps, HTMLDivProps } from '@uiw/utils';
import './style/index.less';
import './style/circle.less';
export declare type Status = 'success' | 'active' | 'exception';
export interface ProgressCircleProps<T> extends IProps, HTMLDivProps {
    type?: IconProps<T>['type'];
    status?: Status;
    showText?: boolean;
    percent?: number;
    width?: number;
    strokeWidth?: number;
    format?: (percent: number) => React.ReactNode;
}
export default class Circle<T> extends React.Component<ProgressCircleProps<T>> {
    static defaultProps: ProgressCircleProps<{}>;
    relativeStrokeWidth(type: 'track' | 'bg' | undefined, elm: SVGPathElement): void;
    render(): JSX.Element;
}
