import React from 'react';
import { DatePickerDayProps, DatePickerDayDateSource } from '@uiw/react-date-picker';
import { IProps } from '@uiw/utils';
import './style/index.less';
export interface CalendarProps extends IProps, DatePickerDayProps {
    /**
     * 设置日历面板上面的日期标题。
     */
    titleFormat?: string;
    /**
     * 点击选择日期回调
     */
    onSelectDay?: (selectDay?: Date, dateSource?: DatePickerDayDateSource) => void;
    /**
     * 日历面板默认展示哪一页
     */
    panelDate?: Date;
    /**
     * 默认高亮当天日期
     */
    today?: Date;
    /**
     * 在日历面板上面添加通知，数组中的对象可以设置 `ElementProps`，如：`style`, `onClick` 等属性。
     */
    data?: ICalendarData[];
    /**
     * 选中的日期
     */
    date?: Date;
    /**
     * `今天` 按钮的文本设置
     */
    todayLabel?: string;
    /**
     * 月份显示文本
     */
    monthLabel?: string[];
    /** 翻页触发事件 */
    onPaging?: (type: 'prev' | 'next' | 'today', month: number, panelDate?: Date) => void;
}
export interface ICalendarData {
    label?: React.ReactNode;
    date?: string;
    [key: string]: any;
}
export default function Calendar(props: CalendarProps): JSX.Element;
