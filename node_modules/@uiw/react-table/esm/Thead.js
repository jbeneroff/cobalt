import _extends from "@babel/runtime/helpers/extends";
import _objectWithoutPropertiesLoose from "@babel/runtime/helpers/objectWithoutPropertiesLoose";
var _excluded = ["prefixCls", "className", "data", "onCellHead"],
    _excluded2 = ["title", "key", "render", "children", "ellipsis"];
import React from 'react';
import { noop } from '@uiw/utils';
import "./style/index.css";
import { jsx as _jsx } from "react/jsx-runtime";
export default (function (props) {
  if (props === void 0) {
    props = {};
  }

  var {
    prefixCls = 'w-table',
    className,
    data = [],
    onCellHead = noop
  } = props,
      other = _objectWithoutPropertiesLoose(props, _excluded);

  return /*#__PURE__*/_jsx("thead", _extends({
    className: [prefixCls, className].filter(Boolean).join(' ').trim()
  }, other, {
    children: data && data.length > 0 && data.map((tds, rowNum) => /*#__PURE__*/_jsx("tr", {
      children: (tds || []).map((item, colNum) => {
        var {
          title,
          ellipsis
        } = item,
            thProps = _objectWithoutPropertiesLoose(item, _excluded2);

        var titleNode = typeof title === 'function' ? title(item, colNum, rowNum) : title;

        if (thProps.colSpan === 0) {
          return null;
        }

        if (ellipsis) {
          thProps.className = (thProps.className || '') + " " + prefixCls + "-ellipsis";
        }

        return /*#__PURE__*/_jsx("th", _extends({
          onClick: evn => onCellHead(item, colNum, rowNum, evn)
        }, thProps, {
          children: titleNode
        }), colNum);
      })
    }, rowNum))
  }));
});
//# sourceMappingURL=Thead.js.map