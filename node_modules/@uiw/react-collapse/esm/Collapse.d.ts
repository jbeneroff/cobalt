import React from 'react';
import { IProps, HTMLDivProps } from '@uiw/utils';
import Panel from './Panel';
import './style/index.less';
export interface CollapseProps extends IProps, Omit<HTMLDivProps, 'onChange'> {
    accordion?: boolean;
    activeKey?: string | string[];
    bordered?: boolean;
    showArrow?: boolean;
    onChange?: (activeKey: string[]) => void;
}
export interface CollapseState {
    activeKey: string[];
}
declare const Collapse: React.ForwardRefExoticComponent<CollapseProps & React.RefAttributes<HTMLDivElement>>;
declare type Collapse = typeof Collapse & {
    Panel: typeof Panel;
};
declare const _default: Collapse;
export default _default;
