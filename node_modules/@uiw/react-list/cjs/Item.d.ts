import React from 'react';
import { IProps } from '@uiw/utils';
declare type ElementTag<T = any> = T extends HTMLElement ? React.HTMLProps<T> : T;
export interface ListItemProps<T = HTMLDivElement> extends IProps, ElementTag {
    disabled?: boolean;
    active?: boolean;
    extra?: React.ReactNode;
    href?: string;
    tagName?: T extends HTMLElement ? keyof JSX.IntrinsicElements : T;
}
declare const Item: React.ForwardRefExoticComponent<Pick<ListItemProps<HTMLDivElement>, keyof ListItemProps<HTMLDivElement>> & React.RefAttributes<HTMLDivElement>>;
export default Item;
