import { CalendarProps } from './';
export declare type RenderDayProps = {
    prefixCls?: string;
    day?: number;
    data?: CalendarProps['data'];
    currentDate?: Date;
};
export default function RenderDay(props: RenderDayProps): JSX.Element;
