import { TableColumns } from './';
export interface ILevelItems {
    header: TableColumns[][];
    ellipsis?: Record<string, boolean>;
    render: {
        [key: string]: any;
    };
}
/**
 * JSON Array => Array
 * @param {Array} date
 */
export declare const getLevelItems: (data: TableColumns[], result?: ILevelItems | undefined) => ILevelItems;
/**
 * Get all columns keys
 * @param {Array} data
 */
export declare const getAllColumnsKeys: (data: TableColumns[], keys?: any[]) => string[];
