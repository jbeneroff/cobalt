import React from 'react';
import { IProps } from '@uiw/utils';
export interface ContentProps extends IProps {
    children?: React.ReactNode;
}
declare const _default: (props?: ContentProps) => JSX.Element;
export default _default;
