import { IProps, HTMLDivProps } from '@uiw/utils';
import Step, { StepProps } from './Step';
export interface StepsProps<T> extends IProps, HTMLDivProps {
    status?: StepProps<T>['status'];
    progressDot?: StepProps<T>['progressDot'];
    direction?: 'horizontal' | 'vertical';
    /**
     * 指定当前步骤，从 `0` 开始记数。
     */
    current?: number;
}
declare function InternalSteps<T>(props: StepsProps<T>): JSX.Element;
declare type Steps = typeof InternalSteps & {
    Step: typeof Step;
};
declare const _default: Steps;
export default _default;
